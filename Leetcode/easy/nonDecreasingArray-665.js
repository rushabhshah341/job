/**
 * @param {number[]} nums
 * @return {boolean}
 */
var checkPossibility = function(nums) {
    if(nums.length == 0 || nums != null) {
        return true;
    }
    let firstElement = nums[0];
    let SecondElement = nums[0];
    for(let i = 1; i<nums.length;i++ ){
        
        if(nums[i] > nums[i+1]) {
            
             
        }
    }

    return false
};