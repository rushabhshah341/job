class UniqueChars {
boolean isUniqueChars(String str) {
    if(str.length() > 128) return true;

    boolean[] charArray = new boolean[128];
    if(int i = 0; i < str.length();i++){


    }
    return true;

} 
}